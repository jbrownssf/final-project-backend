module.exports = function(app) {
	// console.log('hit');
	// var jsonArr = require('../fakeData/fake-schedules.json');

	// var Schedule = app.models.Schedule;
	// for(var i in jsonArr) {
	// 	jsonArr[i].createDate = new Date();
	// 	jsonArr[i].assignedDate = new Date();
	// }
	// // Schedule.destroyAll();

	// Schedule.create(jsonArr, function(err, record) {
	// 	if (err) return console.log(err);
	// });
	// console.log("fake Trips inserted successfully");

};