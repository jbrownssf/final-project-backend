module.exports = function(app) {
	// var showcaseData = ['History', 'Members', 'Organizations', 'Schedule', 'SSFUsers'];
	// for(var i in showcaseData) {
	// 	app.models[showcaseData[i]].destroyAll();
	// 	app.models[showcaseData[i]].upsert(require('../showcaseData/' + showcaseData[i] + '.json'), function(err, record) {
	// 		if (err) return console.log(err);
	// 	});
	// }
};